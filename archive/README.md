# Archive

Dead code retained for historical reference only — none of it is deployed or imported by
live infrastructure. Contents: early POC lambdas (`poc1-crow`, `poc3-murder`, `poc5-api`,
`poc5-murder`, `poc6-worker`) and their CDK stacks (`infra-lib-poc`), the unused
`infra-bin-infra.ts` CDK app entry point (the live entry is `infra/bin/cawnex.ts`), the
superseded `cawnex-auth-stack-enhanced.ts` (superseded once its email-sender logic was
ported into the active auth stack), the standalone `mcp-monarch` prototype, and the unused
`file-processor` orchestration lambda.

Archived 2026-07-01 as part of Task 0.5 (restructure/stage-0).
